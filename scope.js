let bonus = 20;

function sum(first, second) {
    let result = first + second +bonus;
    return result;  
    if (result > 9) {
        const mood = "happy"
        console.log(mood);
    }
}

const output = sum(7, 3); 
console.log(output);